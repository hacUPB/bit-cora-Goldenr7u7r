# Experimento 2

- **¿Qué sucede?**

Primero se digita el número 1 al registro A y se almacena en D, luego se digita el número 2 al registro A, para luego almacenar la suma de el registro A Y D en D, dando esta suma  3. Luego se digita el 16 a la registro A, y se asigna el número que está almacenado en D, en la posición 16 de la RAM, y con la etiqueta (END) y llamandola luego @END se salta a la posición 6.

- **¿Qué valor se almacena en la dirección de memoria 16?**

Se almacena el valor de la suma de A y D, 3

- **¿Por qué crees que es ese valor?**

Es este valor porque se indicó con la instrucción 

@16

M=D

- **¿Qué instrucciones se ejecutan en cada ciclo Fetch-Decode-Execute?**

Todas las instrucciones, ya sean tipo A o tipo C se ejecutan en cada ciclo Fetch-Decode-Execute

- **¿Qué cambios observas en el contenido de la memoria y los registros?**


# Bitacora 2

- **¿Qué diferencia hay entre los datos almacenados en la memoria ROM y en la RAM?**

La memoria ROM almacena/guarda instrucciones mientras que la memoria RAM almacena/guarda datos

